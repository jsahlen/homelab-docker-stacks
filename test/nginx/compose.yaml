services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    environment:
      - APP_ENV=${APP_ENV:-production}
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
